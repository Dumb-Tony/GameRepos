# Node tooling used only for local screenshotting / balance-testing the games
node_modules/
package.json
package-lock.json

# Scratch/working files (screenshots, harness scripts, etc.)
scratchpad/

# playwright test screenshots (suites sometimes run with cwd at repo/game root)
*.png
!tidebound/art/*.png
